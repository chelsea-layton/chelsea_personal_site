/**
 * Custom PM captions for GitHub repos.
 * Key: exact GitHub repo name (slug).
 * Value: one-line product context — the "why" behind the project.
 *
 * Add an entry for each repo you want to feature. Repos without an entry
 * will still appear but with no caption.
 */
export const projectsMeta: Record<string, { caption: string; featured?: boolean }> = {
  // Example entries — replace with your actual repo names and captions:
  // "repo-name": {
  //   caption: "Built to solve X problem for Y type of users.",
  //   featured: true,
  // },
};
